'use strict';

$(document).ready(function () {
    var navUl = $('.navbar__nav-ul'),
        navBtn = $('.navbar__btn'),
        navSvg = $('.navbar__menu-btn');
    navBtn.click(function () {
        navUl.toggleClass('_collapsed');
        navSvg.toggleClass('_collapsed');
    });
});